infect_lib.h - all classes and methods, including Person and Population Objects and definitions

mainPart1 - singular person infection; should work as is, parameters can be changed to

mainPart2 - Steps 2 through 5, with the print formatting embedded and user input included
          - used for the various screenshots in writeup, can activate/deactivate variables
          if necessary

mainExperimental - used for collecting data; all parameter values are manipulated inline
                 - instead of during user input;
                 - commented code was for checking herd immunity, can be changed to collect
                 - any data deemed useful for further experimentation